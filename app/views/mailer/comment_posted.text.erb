# <%= "#{t('mail.body.a_new_comment')} #{@site.name}" %>

<% if @comment.moderation_status != :ok %>
<%= "#{t('mail.body.a_new_comment')} #{admin_comments_url}" %>

<% end %>
<%= t('mail.body.topic') %>     : <%= @comment.topic.title %> (<%= admin_topic_url(@comment.topic) %>)
<%= t('mail.body.name') %>      : <%= @comment.author_name || 'Anonymous' %>
Email     : <%= @comment.author_email || 'None given' %>
IP        : <%= @comment.author_ip %>
User agent: <%= @comment.author_user_agent || 'Unknown' %>
Referer   : <%= @comment.referer || 'Unknown' %>

<%= @comment.content %>
